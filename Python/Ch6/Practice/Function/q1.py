# WAF to print the length of a list. (list is parameter)


num = [4,9,2,3,5,4]

def list_len(list):
    length = len(list)
    return length

print("Total length of the list is : ",list_len(num))